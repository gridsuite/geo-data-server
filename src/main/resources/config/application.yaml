spring:
  application:
    name: geo-data-server

powsybl:
  services:
    network-store-server:
      preloading-strategy: COLLECTION

network-geo-data:
  iterations : 50

powsybl-ws:
  database:
    name: geo_data

max-concurrent-requests: 2
geo_data_round_precision: 5