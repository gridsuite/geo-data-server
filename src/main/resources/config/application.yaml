spring:
  application:
    name: geo-data-server

backing-services:
  network-store-server:
    preloading-strategy: COLLECTION

network-geo-data:
  iterations : 50

powsybl-ws:
  database:
    name: geo_data

